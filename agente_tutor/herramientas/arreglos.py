def arreglos():
    return (
        "Un arreglo es una estructura de datos que permite almacenar varios elementos del "
        "mismo tipo bajo un mismo nombre. Cada elemento ocupa una posición dentro del arreglo "
        "y puede accederse mediante un índice."
    )


def vectores():
    return (
        "Un vector es un arreglo de una sola dimensión que almacena una secuencia de elementos "
        "del mismo tipo. Cada dato ocupa una posición consecutiva y se identifica mediante "
        "un índice que generalmente comienza en cero."
    )


def matrices():
    return (
        "Una matriz es un arreglo de dos dimensiones organizado en filas y columnas. "
        "Cada elemento se identifica mediante dos índices: uno para la fila y otro para "
        "la columna."
    )


def declaracion_arreglos():
    return (
        "La declaración de un arreglo consiste en definir su nombre, el tipo de datos que "
        "almacenará y, en algunos lenguajes, su tamaño. Esto permite reservar el espacio "
        "necesario en memoria para guardar los elementos."
    )


def lectura_escritura():
    return (
        "La lectura y escritura de arreglos consiste en acceder a los elementos mediante "
        "su índice para consultar, modificar o almacenar información dentro del arreglo. "
        "Estas operaciones son fundamentales para trabajar con estructuras de datos."
    )


def operaciones_arreglos():
    return (
        "Las operaciones con arreglos incluyen recorrer sus elementos, buscar información, "
        "modificar valores, ordenar los datos, copiar arreglos y realizar cálculos utilizando "
        "los elementos almacenados."
    )